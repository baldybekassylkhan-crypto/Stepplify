const fs = require('fs'); ['catalog.html', 'map.html'].forEach(f => { let text = fs.readFileSync(f, 'utf8'); text = text.replace('<textarea id="artContent" rows="6" placeholder="Напишите вашу статью здесь..." required></textarea>', '<textarea id="artContent" rows="6" placeholder="Напишите вашу статью здесь..." required></textarea>\n        <div style="text-align: right; font-size: 0.8rem; color: rgba(255,255,255,0.5); margin-top: 4px;" id="artContentCounter">0 / 1000 слов (мин. 500)</div>'); fs.writeFileSync(f, text); });
